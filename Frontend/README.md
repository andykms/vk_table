# Проект таблицы данных

Стек:
Typescript, React, Webpack, SCSS, Jest

# Запуск
Для запуска JSON-server:

```npx json-server db.json```

Для запуска тестов:
 
```npm run test```

Для запуска локального фронтенда:

```npm run start```